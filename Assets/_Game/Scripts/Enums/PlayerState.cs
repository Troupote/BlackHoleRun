using UnityEngine;

namespace BHR
{
    public enum PlayerState { NONE = 0, UI = 1, HUMANOID = 2, SINGULARITY = 3, INACTIVE = 4 }

    public enum  HoleMans {NONE = 0, BLACK_HOLE_MAN = 1, WHITE_HOLE_MAN = 2 }
}
